# macOS
.DS_Store

# Xcode — 构建产物
DerivedData/
build/
*.xcscmblueprint
*.xccheckout
*.moved-aside

# Xcode — 用户状态（xcuserdata 目录按项目约定入库，scheme 已由 xcshareddata 共享）
*.pbxuser
*.perspectivev3
*.xcuserstate

# fastlane — 本机密钥（占位模板 fastlane/.env.example 入库，真实 .env 不入库）
fastlane/.env
# fastlane — 运行报告产物
fastlane/report.xml
